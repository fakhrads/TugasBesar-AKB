<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tugas_besar_akb_if3_cepat_lulus">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tugas_Besar_AKB_IF3_Cepat_Lulus">

        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBLARkGs4zpFEIe4zi2QQAqxmYNf2fxprg" />

        <activity
            android:name=".MapsActivity_2"
            android:exported="false"
            android:label="@string/title_activity_maps2" />
        <activity
            android:name=".AhmadActivity"
            android:exported="false" />
        <activity
            android:name=".FakhriActivity"
            android:exported="false" />
        <activity
            android:name=".FauziActivity"
            android:exported="false" />
        <activity
            android:name=".ViewPager3"
            android:exported="false" />
        <activity
            android:name=".ViewPager2"
            android:exported="false" />
        <activity
            android:name=".ViewPager1"
            android:exported="false" />
        <activity
            android:name=".SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".OjiActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false" />
    </application>

</manifest>